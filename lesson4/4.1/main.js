const num1 = +prompt('input first number') || 0;
const num2 = +prompt('input second number') || 0;
const num3 = +prompt('input third number') || 0;
const mean = (num1 + num2 + num3) / 3;
alert('The arithmetic mean is: ' + mean);
